export const thinkerPath = (slug: string) => `/thinkers/${slug}`;
export const comparisonPath = (left: string, right: string) => `/compare/${left}/${right}`;
export const themePath = (slug: string) => `/themes/${slug}`;
